import styled from "styled-components";

const StyledConfirmButtonWrapper = styled.div`
  display: flex;
  justify-content: center;
  padding-top: 10px;
`;

export default StyledConfirmButtonWrapper;
