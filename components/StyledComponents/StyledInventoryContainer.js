import styled from "styled-components";

const StyledInventoryContainer = styled.div`
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 10px;
  margin: 10px 0;
`;

export default StyledInventoryContainer;
